spring:
  datasource:
    url: jdbc:mysql://localhost:3306/course?characterEncoding=UTF8&autoReconnect=true&useSSL=false
    username: root
    password: 520604
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/*.xml
logging:
  level:
    com:
      kh:
        server:
          mapper: trace
